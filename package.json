{
  "name": "chatdio",
  "version": "1.1.1",
  "type": "module",
  "description": "Web audio library for conversational AI with mic input, device management, WebSocket streaming, and real-time visualization",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "browser": "dist/browser.global.js",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./browser": {
      "types": "./dist/browser.d.ts",
      "import": "./dist/browser.js",
      "require": "./dist/browser.cjs"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts src/browser.ts --format cjs,esm --dts --clean && tsup src/browser.ts --format iife --global-name ConversationalAudio --outDir dist",
    "build:lib": "tsup src/index.ts --format cjs,esm --dts --clean",
    "build:browser": "tsup src/browser.ts --format iife --global-name ConversationalAudio --outDir dist",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "typecheck": "tsc --noEmit",
    "example": "node example/server.js"
  },
  "keywords": [
    "web-audio",
    "microphone",
    "conversational-ai",
    "websocket",
    "audio-streaming",
    "echo-cancellation"
  ],
  "license": "MIT",
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.6",
    "@hono/node-ws": "^1.2.0",
    "hono": "^4.10.6"
  },
  "files": [
    "dist"
  ]
}